import { useEffect,useMemo,useRef } from 'react'
import L from 'leaflet'
import 'leaflet/dist/leaflet.css'
import { formatAccuracy,formatDistance,geoClassFor,geoClassLabel,routeColorFor,type TrackingMapMode } from '../lib/trackingGeo'
import '../styles/tracking.css'
import '../styles/tracking-hotfix.css'

export type TrackingSnapshot={
  route_plan_id:string;route_session_id?:string|null;employee_id:string;full_name:string;job_title?:string|null;route_date:string;tracking_status:string;journey_status?:string|null;
  planned_clients:number;visited_clients:number;coverage_pct:number;route_window_seconds:number;visit_seconds:number;estimated_distance_m:number;
  last_event_id?:string|null;last_event_type?:string|null;last_event_at?:string|null;last_latitude?:number|null;last_longitude?:number|null;last_accuracy_m?:number|null;last_subject_name?:string|null;last_event_label?:string|null;last_event_age_minutes?:number|null;freshness_status:string;
  last_gps_quality?:string|null;last_gps_reliable?:boolean|null;last_location_exception_code?:string|null;last_location_exception_text?:string|null;
  next_route_stop_id?:string|null;next_stop_order?:number|null;next_subject_name?:string|null;next_latitude?:number|null;next_longitude?:number|null;
  official_regions?:string[]|null;official_provinces?:string[]|null;official_municipalities?:string[]|null;
}
export type TrackingStop={route_stop_id:string;route_plan_id:string;route_date:string;employee_id:string;full_name:string;stop_order:number;stop_status:string;subject_name:string;latitude?:number|null;longitude?:number|null;official_region?:string|null;official_province?:string|null;official_municipality?:string|null}
export type TrackingEvent={event_id:string;route_plan_id:string;route_session_id?:string|null;employee_id:string;full_name:string;route_date:string;event_type:string;event_at:string;latitude?:number|null;longitude?:number|null;accuracy_m?:number|null;subject_name?:string|null;stop_order?:number|null;route_stop_id?:string|null;event_label:string;has_gps:boolean;raw_has_gps?:boolean;gps_quality?:string|null;distance_to_target_m?:number|null;location_exception_code?:string|null;location_exception_text?:string|null}

type Props={
 snapshots:TrackingSnapshot[];stops:TrackingStop[];events:TrackingEvent[];selectedRoutePlanId?:string;selectedEventId?:string;playbackIndex:number;showStops:boolean;viewMode:TrackingMapMode;isolateSelected:boolean;
 onPlanSelect?:(routePlanId:string)=>void;onEventSelect?:(eventId:string)=>void
}
const CENTER:[number,number]=[18.7357,-70.1627]
const BOUNDS=L.latLngBounds([16.85,-72.85],[20.55,-67.45])
const statusColor=(status:string)=>status==='EN_VISITA'?'#159a66':status==='EN_TRASLADO'?'#2563eb':status==='EVENTUALIDAD'?'#d97706':status==='PENDIENTE_CIERRE'?'#c71f2d':status==='FINALIZADA'?'#64748b':status==='NO_EJECUTADA'?'#a9464d':'#8b9aad'
const stopFill=(status:string)=>status==='VISITADO'?'#16a34a':status==='EN_VISITA'?'#0ea5e9':status==='NO_VISITADO'?'#dc2626':status==='REPROGRAMADO'?'#f59e0b':status==='CANCELADO'?'#64748b':'#ffffff'
const qualityFill=(value:string)=>value==='ON_SITE'?'#16a34a':value==='NEAR'?'#0284c7':value==='OUTSIDE'?'#d97706':value==='DISTANT'?'#dc2626':value==='UNRELIABLE'?'#a16207':'#64748b'
const esc=(value:unknown)=>String(value??'').replace(/[&<>'"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]||c))
const eventOrder=(a:TrackingEvent,b:TrackingEvent)=>new Date(a.event_at).getTime()-new Date(b.event_at).getTime()

export function LiveTrackingMap({snapshots,stops,events,selectedRoutePlanId='',selectedEventId='',playbackIndex=-1,showStops,viewMode,isolateSelected,onPlanSelect,onEventSelect}:Props){
 const host=useRef<HTMLDivElement|null>(null),mapRef=useRef<L.Map|null>(null),positionsRef=useRef<L.LayerGroup|null>(null),stopsRef=useRef<L.LayerGroup|null>(null),routesRef=useRef<L.LayerGroup|null>(null),comparisonRef=useRef<L.LayerGroup|null>(null)
 const employeeIds=useMemo(()=>Array.from(new Set([...snapshots.map(s=>s.employee_id),...stops.map(s=>s.employee_id),...events.map(e=>e.employee_id)])).sort(),[snapshots,stops,events])
 const selectedEvents=useMemo(()=>events.filter(e=>e.route_plan_id===selectedRoutePlanId&&e.has_gps&&e.latitude!=null&&e.longitude!=null).sort(eventOrder),[events,selectedRoutePlanId])
 const selectedEvent=useMemo(()=>events.find(e=>e.event_id===selectedEventId)||null,[events,selectedEventId])
 const selectedStop=useMemo(()=>selectedEvent?.route_stop_id?stops.find(s=>s.route_stop_id===selectedEvent.route_stop_id)||null:null,[stops,selectedEvent])
 const visibleSnapshots=useMemo(()=>isolateSelected&&selectedRoutePlanId?snapshots.filter(s=>s.route_plan_id===selectedRoutePlanId):snapshots,[snapshots,isolateSelected,selectedRoutePlanId])
 const visibleStops=useMemo(()=>isolateSelected&&selectedRoutePlanId?stops.filter(s=>s.route_plan_id===selectedRoutePlanId):stops,[stops,isolateSelected,selectedRoutePlanId])
 const visibleEvents=useMemo(()=>isolateSelected&&selectedRoutePlanId?events.filter(e=>e.route_plan_id===selectedRoutePlanId):events,[events,isolateSelected,selectedRoutePlanId])

 useEffect(()=>{if(!host.current||mapRef.current)return;const map=L.map(host.current,{zoomControl:false,preferCanvas:true,minZoom:7,maxBounds:BOUNDS,maxBoundsViscosity:.65}).setView(CENTER,8.5);L.control.zoom({position:'bottomright'}).addTo(map);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:20,maxNativeZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);positionsRef.current=L.layerGroup().addTo(map);stopsRef.current=L.layerGroup().addTo(map);routesRef.current=L.layerGroup().addTo(map);comparisonRef.current=L.layerGroup().addTo(map);mapRef.current=map;requestAnimationFrame(()=>map.invalidateSize());return()=>{map.remove();mapRef.current=null}},[])

 useEffect(()=>{const map=mapRef.current,layer=positionsRef.current;if(!map||!layer)return;layer.clearLayers();const points:L.LatLngExpression[]=[];visibleSnapshots.forEach(s=>{if(s.last_latitude==null||s.last_longitude==null)return;const selected=s.route_plan_id===selectedRoutePlanId,color=routeColorFor(s.employee_id,employeeIds),state=statusColor(s.tracking_status);points.push([s.last_latitude,s.last_longitude]);const marker=L.circleMarker([s.last_latitude,s.last_longitude],{radius:selected?12:9,weight:selected?5:4,color,fillColor:state,fillOpacity:.96});const age=s.last_event_age_minutes==null?'sin hora':s.last_event_age_minutes<1?'ahora':`hace ${s.last_event_age_minutes} min`;marker.bindTooltip(`<div class="tracking-map-tooltip"><b>${esc(s.full_name)}</b><span>${esc(s.tracking_status.replaceAll('_',' '))}</span><span>${esc(s.last_event_label||'Último registro')} · ${esc(age)}</span>${s.last_subject_name?`<strong>${esc(s.last_subject_name)}</strong>`:''}<small>${s.last_accuracy_m!=null?`${esc(formatAccuracy(s.last_accuracy_m))} · `:''}último evento GPS confiable</small></div>`,{direction:'top',opacity:.98});marker.on('click',()=>onPlanSelect?.(s.route_plan_id));marker.addTo(layer);L.marker([s.last_latitude,s.last_longitude],{interactive:false,icon:L.divIcon({className:'tracking-initial-marker',html:`<span style="background:${color}">${esc(s.full_name.slice(0,1).toUpperCase())}</span>`,iconSize:[22,22],iconAnchor:[11,11]})}).addTo(layer)});if(viewMode==='LIVE'&&!selectedRoutePlanId&&points.length){const bounds=L.latLngBounds(points);if(bounds.isValid())map.fitBounds(bounds.pad(.22),{maxZoom:13})}},[visibleSnapshots,selectedRoutePlanId,employeeIds,onPlanSelect,viewMode])

 useEffect(()=>{const layer=stopsRef.current;if(!layer)return;layer.clearLayers();if(!showStops)return;visibleStops.forEach(s=>{if(s.latitude==null||s.longitude==null)return;const color=routeColorFor(s.employee_id,employeeIds),stopEvents=visibleEvents.filter(e=>e.route_stop_id===s.route_stop_id).sort(eventOrder),lastStopEvent=stopEvents.length?stopEvents[stopEvents.length-1]:undefined,anomaly=lastStopEvent?.location_exception_code;const fill=stopFill(s.stop_status),pending=s.stop_status==='PENDIENTE',symbol=s.stop_status==='VISITADO'?'✓':s.stop_status==='EN_VISITA'?'▶':String(s.stop_order);const html=`<div class="tracking-stop-pin${pending?' pending':''}${s.route_plan_id===selectedRoutePlanId?' selected':''}" style="--seller:${color};--state:${fill}"><span>${esc(symbol)}</span>${anomaly?'<em>!</em>':''}</div>`;const marker=L.marker([s.latitude,s.longitude],{icon:L.divIcon({className:'tracking-stop-divicon',html,iconSize:[30,30],iconAnchor:[15,15]})});marker.bindTooltip(`<div class="tracking-map-tooltip"><b>${s.stop_order}. ${esc(s.subject_name)}</b><span>${esc(s.stop_status.replaceAll('_',' '))}</span><span>${esc(s.full_name)}</span>${anomaly?`<small>${esc(lastStopEvent?.location_exception_code||'Anomalía geográfica')}</small>`:''}</div>`,{direction:'top',opacity:.97});marker.on('click',()=>onPlanSelect?.(s.route_plan_id));marker.addTo(layer)})},[visibleStops,visibleEvents,employeeIds,selectedRoutePlanId,showStops,onPlanSelect])

 useEffect(()=>{const map=mapRef.current,layer=routesRef.current;if(!map||!layer)return;layer.clearLayers();if(viewMode==='LIVE'&&!selectedRoutePlanId)return;const planIds=Array.from(new Set([...visibleStops.map(s=>s.route_plan_id),...visibleEvents.map(e=>e.route_plan_id)]));const boundsPoints:L.LatLngExpression[]=[];planIds.forEach(planId=>{const planStops=visibleStops.filter(s=>s.route_plan_id===planId&&s.latitude!=null&&s.longitude!=null).sort((a,b)=>a.stop_order-b.stop_order),planEvents=visibleEvents.filter(e=>e.route_plan_id===planId&&e.has_gps&&e.latitude!=null&&e.longitude!=null).sort(eventOrder),routeEvents=viewMode==='ROUTES'?planEvents.filter(e=>{const geo=geoClassFor(e);return geo!=='DISTANT'&&geo!=='UNRELIABLE'}):planEvents;const employeeId=planStops[0]?.employee_id||routeEvents[0]?.employee_id||planEvents[0]?.employee_id||'',color=routeColorFor(employeeId,employeeIds),selected=planId===selectedRoutePlanId,dim=Boolean(selectedRoutePlanId&&!selected);const plannedPoints=planStops.map(s=>[s.latitude!,s.longitude!] as [number,number]),actualPoints=routeEvents.map(e=>[e.latitude!,e.longitude!] as [number,number]);if(viewMode==='ROUTES')boundsPoints.push(...(plannedPoints.length?plannedPoints:actualPoints));if(plannedPoints.length>1)L.polyline(plannedPoints,{color,weight:selected?3:2,opacity:dim?.16:selected?.62:.34,dashArray:'2 7'}).bindTooltip('Secuencia planificada entre paradas · no representa navegación vial').addTo(layer);if(actualPoints.length>1)L.polyline(actualPoints,{color,weight:selected?5:3,opacity:dim?.18:selected?.94:.72,dashArray:'10 7'}).bindTooltip('Unión estimada entre eventos GPS registrados y geográficamente coherentes con su parada').addTo(layer);if(viewMode!=='LIVE')routeEvents.forEach(e=>{const geo=geoClassFor(e),marker=L.circleMarker([e.latitude!,e.longitude!],{radius:e.event_id===selectedEventId?8:selected?5:4,weight:e.event_id===selectedEventId?4:2,color,fillColor:viewMode==='QUALITY'?qualityFill(geo):color,fillOpacity:dim?.25:.92});marker.bindTooltip(`<div class="tracking-map-tooltip"><b>${esc(e.event_label)}</b><span>${new Date(e.event_at).toLocaleTimeString('es-DO',{hour:'2-digit',minute:'2-digit'})}${e.subject_name?` · ${esc(e.subject_name)}`:''}</span><small>${esc(formatAccuracy(e.accuracy_m))}${e.distance_to_target_m!=null?` · ${esc(formatDistance(e.distance_to_target_m))} al cliente`:''}</small><strong>${esc(geoClassLabel(geo))}</strong></div>`,{direction:'top'});marker.on('click',()=>onEventSelect?.(e.event_id));marker.addTo(layer)});const start=routeEvents.find(e=>e.event_type==='ROUTE_START'),finish=[...routeEvents].reverse().find(e=>e.event_type==='ROUTE_END');if(start)L.marker([start.latitude!,start.longitude!],{icon:L.divIcon({className:'tracking-endpoint-divicon',html:`<span style="--seller:${color}">S</span>`,iconSize:[28,28],iconAnchor:[14,14]})}).addTo(layer);if(finish)L.marker([finish.latitude!,finish.longitude!],{icon:L.divIcon({className:'tracking-endpoint-divicon',html:`<span style="--seller:${color}">F</span>`,iconSize:[28,28],iconAnchor:[14,14]})}).addTo(layer)});if(viewMode==='ROUTES'&&!selectedEventId&&playbackIndex<0&&boundsPoints.length){const bounds=L.latLngBounds(boundsPoints);if(bounds.isValid())map.fitBounds(bounds.pad(.16),{maxZoom:15})}},[visibleStops,visibleEvents,employeeIds,selectedRoutePlanId,selectedEventId,viewMode,playbackIndex,onEventSelect])

 useEffect(()=>{const map=mapRef.current,layer=comparisonRef.current;if(!map||!layer)return;layer.clearLayers();if(!selectedEvent||selectedEvent.latitude==null||selectedEvent.longitude==null||!selectedStop||selectedStop.latitude==null||selectedStop.longitude==null)return;const color=routeColorFor(selectedEvent.employee_id,employeeIds),a:[number,number]=[selectedEvent.latitude,selectedEvent.longitude],b:[number,number]=[selectedStop.latitude,selectedStop.longitude],geo=geoClassFor(selectedEvent);L.polyline([a,b],{color:'#111827',weight:3,opacity:.72,dashArray:'5 7'}).bindTooltip(`${formatDistance(selectedEvent.distance_to_target_m)} entre registro y punto maestro`).addTo(layer);L.marker(a,{icon:L.divIcon({className:'tracking-compare-divicon',html:`<span class="record" style="--seller:${color}">R</span>`,iconSize:[34,34],iconAnchor:[17,17]})}).bindTooltip(`<div class="tracking-map-tooltip"><b>Lugar del registro</b><span>${esc(selectedEvent.subject_name||selectedEvent.event_label)}</span><small>${esc(formatAccuracy(selectedEvent.accuracy_m))} · ${esc(geoClassLabel(geo))}</small></div>`).addTo(layer);L.marker(b,{icon:L.divIcon({className:'tracking-compare-divicon',html:'<span class="client">C</span>',iconSize:[34,34],iconAnchor:[17,17]})}).bindTooltip(`<div class="tracking-map-tooltip"><b>Ubicación maestra del cliente</b><span>${esc(selectedStop.subject_name)}</span><small>${esc(selectedStop.official_province||'')} ${esc(selectedStop.official_municipality||'')}</small></div>`).addTo(layer);const bounds=L.latLngBounds([a,b]);if(bounds.isValid())map.fitBounds(bounds.pad(.24),{maxZoom:15})},[selectedEvent,selectedStop,employeeIds])

 useEffect(()=>{const map=mapRef.current;if(!map||!selectedRoutePlanId||playbackIndex<0||!selectedEvents.length)return;const current=selectedEvents[Math.min(playbackIndex,selectedEvents.length-1)];map.flyTo([current.latitude!,current.longitude!],Math.max(map.getZoom(),14),{duration:.45})},[selectedRoutePlanId,selectedEvents,playbackIndex])

 const legend=useMemo(()=>visibleSnapshots.slice(0,8),[visibleSnapshots])
 return <div className="tracking-map-shell"><div ref={host} className="tracking-map-host"/>{viewMode==='ROUTES'&&legend.length>0&&<div className="tracking-route-legend"><b>Rutas visibles</b>{legend.map(s=><button key={s.route_plan_id} className={selectedRoutePlanId===s.route_plan_id?'selected':''} onClick={()=>onPlanSelect?.(s.route_plan_id)}><i style={{background:routeColorFor(s.employee_id,employeeIds)}}/><span>{s.full_name}</span><small>{s.visited_clients}/{s.planned_clients}</small></button>)}{visibleSnapshots.length>8&&<small>+{visibleSnapshots.length-8} jornada(s)</small>}</div>}<div className="tracking-map-legend"><span><i className="dot visit"/>En visita</span><span><i className="dot transit"/>En traslado</span><span><i className="dot stale"/>Alerta</span>{viewMode==='ROUTES'&&<><small>Línea fina punteada = secuencia planificada · línea gruesa discontinua = unión estimada entre registros GPS coherentes con su parada.</small><small>Los registros distantes se auditan en Calidad GPS y no deforman la vista del recorrido.</small><small>El color identifica al vendedor; el interior de cada parada identifica su estado.</small></>}{viewMode==='QUALITY'&&<small>R = lugar del registro · C = ubicación maestra del cliente. La comparación no bloquea la gestión.</small>}</div></div>
}
