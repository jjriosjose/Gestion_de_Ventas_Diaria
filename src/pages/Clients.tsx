import { useEffect,useState } from 'react'
import { ChevronLeft,ChevronRight,MapPin,Navigation,PenLine,Search,X } from 'lucide-react'
import { supabase } from '../lib/supabase'
import { googleMapsNavigation } from '../lib/geo'
import { exportPdf,exportXlsx } from '../lib/export'
import type { Client } from '../types'
import { useAuth } from '../context/AuthContext'
const PAGE=50
export function Clients(){const {employee}=useAuth();const admin=['Administrador','Supervisor'].includes(employee?.app_role||'');const [rows,setRows]=useState<Client[]>([]);const [q,setQ]=useState('');const [company,setCompany]=useState('');const [v,setV]=useState('');const [g,setG]=useState('');const [page,setPage]=useState(0);const [total,setTotal]=useState(0);const [loading,setLoading]=useState(true);const [edit,setEdit]=useState<Client|null>(null)
 const load=async()=>{setLoading(true);let req=supabase.from('clients').select('id,company_code,codempr,cod_empresa,v_cartera,g_cartera,region,province,municipality,client_type,legal_name,contact_name,address1,display_name,phone1,phone2,mobile,email,longitude,latitude,active_status,last_invoice_date,last_invoice_amount,credit_days,geo_status,geo_verified_at',{count:'exact'});if(q.trim())req=req.or(`legal_name.ilike.%${q.trim()}%,codempr.ilike.%${q.trim()}%,phone1.ilike.%${q.trim()}%,mobile.ilike.%${q.trim()}%`);if(company)req=req.eq('company_code',company);if(v)req=req.eq('v_cartera',v);if(g)req=req.eq('g_cartera',g);const {data,count}=await req.order('legal_name').range(page*PAGE,page*PAGE+PAGE-1);setRows((data||[]) as Client[]);setTotal(count||0);setLoading(false)}
 useEffect(()=>{const t=setTimeout(()=>void load(),250);return()=>clearTimeout(t)},[q,company,v,g,page])
 const exportAll=async(kind:'xlsx'|'pdf')=>{let req=supabase.from('clients').select('codempr,company_code,legal_name,v_cartera,g_cartera,region,province,municipality,phone1,mobile,email,latitude,longitude,active_status,geo_status');if(q.trim())req=req.or(`legal_name.ilike.%${q.trim()}%,codempr.ilike.%${q.trim()}%`);if(company)req=req.eq('company_code',company);if(v)req=req.eq('v_cartera',v);if(g)req=req.eq('g_cartera',g);const {data}=await req.order('legal_name').limit(5000);const out=(data||[]).map((r:any)=>({Codigo:r.codempr,Empresa:r.company_code,'Razon Social':r.legal_name,'V-CARTERA':r.v_cartera,'G-CARTERA':r.g_cartera,Region:r.region,Provincia:r.province,Municipio:r.municipality,Telefono:r.phone1,Celular:r.mobile,Email:r.email,Latitud:r.latitude,Longitud:r.longitude,Estado:r.active_status,'Calidad Geo':r.geo_status}));if(kind==='xlsx')await exportXlsx('Maestro_Clientes',out);else exportPdf('Maestro de Clientes',out)}
 return <div className="page-stack"><div className="page-head"><div><span className="eyebrow">MAESTRO EDITABLE</span><h2>Clientes</h2><p>Consulta centralizada de la cartera vigente.</p></div><div className="button-row"><button className="secondary" onClick={()=>void exportAll('xlsx')}>Excel</button><button className="secondary" onClick={()=>void exportAll('pdf')}>PDF</button></div></div><div className="filter-bar"><div className="search-field"><Search size={18}/><input placeholder="Buscar razón social, código o teléfono..." value={q} onChange={e=>{setQ(e.target.value);setPage(0)}}/></div><select value={company} onChange={e=>{setCompany(e.target.value);setPage(0)}}><option value="">Todas las empresas</option><option>KARAKA</option><option>DISTRIBUIDORA</option></select><input placeholder="V-CARTERA exacto" value={v} onChange={e=>{setV(e.target.value);setPage(0)}}/><input placeholder="G-CARTERA exacto" value={g} onChange={e=>{setG(e.target.value);setPage(0)}}/></div><div className="panel table-panel"><div className="table-meta"><b>{total.toLocaleString()} clientes</b><span>Página {page+1} de {Math.max(1,Math.ceil(total/PAGE))}</span></div><div className="responsive-table"><table><thead><tr><th>Cliente</th><th>Empresa</th><th>V-CARTERA</th><th>G-CARTERA</th><th>Ubicación</th><th>Contacto</th><th>Acciones</th></tr></thead><tbody>{loading?<tr><td colSpan={7}><div className="skeleton"/></td></tr>:rows.map(c=><tr key={c.id}><td data-label="Cliente"><b>{c.legal_name}</b><small>{c.codempr}</small></td><td data-label="Empresa"><span className="badge">{c.company_code||'—'}</span></td><td data-label="V-CARTERA">{c.v_cartera||'—'}</td><td data-label="G-CARTERA">{c.g_cartera||'—'}</td><td data-label="Ubicación"><span>{c.municipality||c.province||'—'}</span><small>{c.latitude!=null?<><MapPin size={12}/> Georreferenciado · {geoLabel(c.geo_status)}</>:'Sin coordenadas · SIN GEO'}</small></td><td data-label="Contacto"><span>{c.phone1||c.mobile||'—'}</span><small>{c.contact_name||''}</small></td><td data-label="Acciones"><div className="row-actions">{googleMapsNavigation(c.latitude,c.longitude)&&<a className="icon-btn" href={googleMapsNavigation(c.latitude,c.longitude)!} target="_blank" rel="noreferrer" title="Navegar"><Navigation size={17}/></a>}{admin&&<button className="icon-btn" onClick={()=>setEdit(c)} title="Editar"><PenLine size={17}/></button>}</div></td></tr>)}</tbody></table></div><div className="pagination"><button disabled={page===0} onClick={()=>setPage(p=>p-1)}><ChevronLeft/></button><span>{page+1}</span><button disabled={(page+1)*PAGE>=total} onClick={()=>setPage(p=>p+1)}><ChevronRight/></button></div></div>{edit&&<EditClient client={edit} onClose={()=>setEdit(null)} onSaved={()=>{setEdit(null);void load()}}/>}</div>}
function geoLabel(status?:string|null){return status==='VERIFICADA'?'Verificada':status==='POSIBLE_ERROR'?'Posible error':status==='SIN_GEO'?'Sin geo':'Sin verificar'}
function EditClient({client,onClose,onSaved}:{client:Client;onClose:()=>void;onSaved:()=>void}){const [f,setF]=useState({...client});const [busy,setBusy]=useState(false);const save=async()=>{setBusy(true);const {error}=await supabase.from('clients').update({legal_name:f.legal_name,contact_name:f.contact_name,address1:f.address1,phone1:f.phone1,phone2:f.phone2,mobile:f.mobile,email:f.email,v_cartera:f.v_cartera,g_cartera:f.g_cartera,region:f.region,province:f.province,municipality:f.municipality,latitude:f.latitude==null?null:Number(f.latitude),longitude:f.longitude==null?null:Number(f.longitude),active_status:f.active_status}).eq('id',client.id);setBusy(false);if(error)alert(error.message);else onSaved()};const set=(k:keyof Client,v:any)=>setF(x=>({...x,[k]:v}));return <div className="modal-wrap"><button className="modal-backdrop" onClick={onClose}/><div className="modal large"><div className="modal-head"><div><span className="eyebrow">EDITAR MAESTRO</span><h3>{client.legal_name}</h3></div><button className="icon-btn" onClick={onClose}><X/></button></div><div className="form-grid"><label>Razón social<input value={f.legal_name||''} onChange={e=>set('legal_name',e.target.value)}/></label><label>Contacto<input value={f.contact_name||''} onChange={e=>set('contact_name',e.target.value)}/></label><label>Teléfono<input value={f.phone1||''} onChange={e=>set('phone1',e.target.value)}/></label><label>Celular<input value={f.mobile||''} onChange={e=>set('mobile',e.target.value)}/></label><label>V-CARTERA<input value={f.v_cartera||''} onChange={e=>set('v_cartera',e.target.value)}/></label><label>G-CARTERA<input value={f.g_cartera||''} onChange={e=>set('g_cartera',e.target.value)}/></label><label>Región<input value={f.region||''} onChange={e=>set('region',e.target.value)}/></label><label>Provincia<input value={f.province||''} onChange={e=>set('province',e.target.value)}/></label><label>Municipio<input value={f.municipality||''} onChange={e=>set('municipality',e.target.value)}/></label><label>Latitud<input type="number" step="any" value={f.latitude??''} onChange={e=>set('latitude',e.target.value===''?null:Number(e.target.value))}/></label><label>Longitud<input type="number" step="any" value={f.longitude??''} onChange={e=>set('longitude',e.target.value===''?null:Number(e.target.value))}/></label><label className="span-2">Dirección<input value={f.address1||''} onChange={e=>set('address1',e.target.value)}/></label></div><div className="modal-actions"><button className="secondary" onClick={onClose}>Cancelar</button><button className="primary" disabled={busy} onClick={()=>void save()}>{busy?'Guardando...':'Guardar cambios'}</button></div></div></div>}
